// allow tylko właścicielowi
rules_version = '2';
service cloud.firestore {
  match /databases/{db}/documents {
    function isUser(uid){ return request.auth != null && request.auth.uid == uid; }
    match /users/{uid}/{col=**}/{doc} {
      allow read, write: if isUser(uid);
    }
  }
}